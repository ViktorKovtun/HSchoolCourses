let firstAmount = +prompt("Enter the first number:");
let secondAmount = +prompt("Enter the second number:");
(firstAmount % secondAmount === 0) ? console.log("Divisible") : console.log("Divisible with remainder");