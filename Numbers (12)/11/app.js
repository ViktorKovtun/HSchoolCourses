let firstAmount = +prompt("Enter the first number:");
let secondAmount = +prompt("Enter the second number:");
console.log("Sum:", firstAmount + secondAmount);
console.log("Dif:", firstAmount - secondAmount);
console.log("Prod:", firstAmount * secondAmount);